// js/components/table.js:
// Reusable data-table component: checkbox selection, column sort hints, and a
// bulk-action bar. Server-rendered rows are swapped in via HTMX; this module
// owns only client-side selection state.
window.App = window.App || {};

window.App.table = (() => {
  return {};
})();