// js/components/filters.js:
// Filter bar, saved-filter picker, and repo/org checkbox popovers. Used by the
// queue and inbox views; the heavy filtering happens server-side.
window.App = window.App || {};

window.App.filters = (() => {
  return {};
})();